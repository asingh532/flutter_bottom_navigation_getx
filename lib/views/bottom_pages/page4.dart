import 'package:flutter/material.dart';

class PageFourth extends StatelessWidget {
  const PageFourth({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Page Fourth"),
    );
  }
}
